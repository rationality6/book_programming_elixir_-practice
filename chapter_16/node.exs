# iex -sname node_one --cookie chocolate-chip
# iex -sname node_two --cookie chocolate-chip

# Node.self

# Node.connect("")

# Node.list

# func = fn -> IO.puts(Enum.join(File.ls!," ,")) end

# Node.spawn(:"node_two@Hyuns-MacBook-Air", func)
